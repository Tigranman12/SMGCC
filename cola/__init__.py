"""COLA compiler for LW_OS — compiles COLA source to COLA-VM bytecode.

Implements the unbuilt M7 milestone of the LW_OS roadmap: a compiler that turns
COLA `.cola` source into bytecode the on-OS `cola_vm` executes.
"""
